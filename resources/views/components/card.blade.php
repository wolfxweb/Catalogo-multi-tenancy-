<div class="{{ $classCard }}">
    <div class="card">
        <div class="{{ $color }} card-header">
            {{ $titulo }}
        </div>
        <div class="card-body">
            {{ $slot }}
        </div>
    </div>
</div>
