<div class="form-check form-check-inline">
    <input class="form-check-input" type="radio" name="{{$name}}" id="{{$id}}" value="{{$value}}" {{ $value == $checked ? 'checked':''}}>
    <label class="form-check-label" for="{{$id}}">{{$titulo}}</label>
 </div>
